#!/bin/bash
# Stop hook — reminds the session to close out per the NVG boot contract.
echo "CLOSE CHECK: presence close row? apartment row? Decisions/Learnings written? council row for any PR? Slack close line under your name?"
exit 0
